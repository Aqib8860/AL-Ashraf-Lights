export type ProductTableSchema = {
    id: number,
    name: string,
    category: string,
    price: number,
    is_available: boolean,
    quantity: number,
}

export type ProductCardSchema = {
    id: number,
    name: string,
    description: string,
    price: number,
    category: string,
    is_available: boolean,
    image: string,
    quantity: number,
}

